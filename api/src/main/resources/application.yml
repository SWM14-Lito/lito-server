spring:
  profiles:
    active: local
    include: core

  servlet:
    multipart:
      max-file-size: 10MB

---
spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: dev